"""Give a single command that computes the sum from Exercise R-1.4, relying on Pythons comprehension syntax and the built-in sum function"""
# def sumSquares(n):
#   return sum(x*x for x in range(1, n))

# def sumSquares(n):
#   return sum(x**2 for x in range(n))


print(sumSquares(10))
